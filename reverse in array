#include<stdio.h>
#define MAX_SIZE 1000
int main()
{
    int a[MAX_SIZE],n,i;
    scanf("%d",&n);      //aray_size
    for(i=0; i<n; i++)
    {
        scanf("%d",&a[i]);  //input  array value
    }
    for(i=n-1; i>=0; i--) //reverse
    {
        printf("%d\t",a[i]);
    }

    return 0;
}
