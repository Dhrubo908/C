#include<stdio.h>
#define MAX_SIZE 100
int main()
{
    int i,n;
    int arr[MAX_SIZE];
    scanf("%d",&n);

    for(i=0; i<n; i++)
    {
        printf("Enter value until off ");
        scanf("%d",&arr[i]);
    }
    for(i=0; i<n; i++)
    {
        if(arr[i]>0)
            printf("%d\n",arr[i]);
    }
    return 0;
}


