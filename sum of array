#include<stdio.h>
#define MAX_SIZE 1000
int main()
{
    int a[MAX_SIZE],n,i,sum=0;
    scanf("%d",&n);
    for(i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }

    for(i=0; i<n; i++)
    {
        sum=sum+a[i];
    }

    printf("%d  \t",sum);


    return 0;
}

