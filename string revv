#include<stdio.h>
int main()
{
int i=0,len=0;
    char s1[30];

    gets(s1);
    while(s1[i]!='\0')
    {
        i++;
        len++;
    }
    for(i=len-1;i>=0;i--)
    {
        printf("%c",s1[i]);
    }
   return 0;
}
